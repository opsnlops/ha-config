#
# These are now defined in the modern template: section of configuration.yaml
# This file is kept because configuration.yaml references it via light: !include lights.yaml
# but it no longer contains any legacy template lights.
[]
