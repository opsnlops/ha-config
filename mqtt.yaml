#
# The broker currently runs as a process on the HASS host
#

broker: 127.0.0.1
discovery: true
client_id: rex-homeassistant
keepalive: 120
birth_message:
  topic: "home/homeassistant/status"
  payload: "online"
will_message:
  topic: "home/homeassistant/status"
  payload: "offline"
