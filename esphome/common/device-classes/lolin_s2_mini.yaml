esphome:
  name: ${devicename}

esp32:
  board: esp32dev
  variant: esp32s2
  framework:
    type: arduino
    version: latest


preferences:
  flash_write_interval: 10min

logger:

packages:
  mqtt: !include  ../mqtt.yaml
